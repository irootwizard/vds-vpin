回滚「prompt.txt 前端改造」前的静态资源：

  位置：与本文件同目录下的 vpin.pre-prompt-backup（完整副本）

  PowerShell（在 public 目录执行）：

  Remove-Item -Recurse -Force .\vpin
  Copy-Item -Recurse -Force .\vpin.pre-prompt-backup .\vpin

  然后重新构建/刷新 Tauri 或 Vite 即可。
