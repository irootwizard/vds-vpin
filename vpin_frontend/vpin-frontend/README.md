# DEPRECATED — use vpin-console

此目录为 **legacy** 前端，默认入口已迁移至仓库根目录 [`vpin-console/`](../../vpin-console/)。

仅用于开发回溯：`$env:VPIN_UI='legacy'` 后使用原 `start-ahe.ps1` 逻辑（若仍配置）。

**请勿**在此目录新增 TEMP-DEMO 或新功能。
