# 任务完成清单（2026-06-04）

> **设计定稿（2026-06-10）：** [`cp-snark-分层证明与RLC设计定稿.md`](./cp-snark-分层证明与RLC设计定稿.md) — 按层 π；`mac_rlc` 桩非目标。  
> 对应 [`综合未来工作路线图.md`](./综合未来工作路线图.md) 阶段 0 及 `layer_proof` 模块落点。  
> 验证：`cd src/cp-snark-full && cargo test`

---

## 阶段 0 — 密码学基础闭合

### 0.1 P0：`embed_u128_to_scalar` 与 witness 编码一致性

| 项 | 状态 | 说明 |
|----|------|------|
| 选定 cp-snark-full **规范编码** | ✅ | 承诺 / RLC / 未来 L1 统一使用 `curve::embed_u128_to_scalar`（16 字节 LE + `from_bytes_wide`） |
| witness 路径（`point_mult.rs`） | ✅ 保持不改 | 仍用 `dalek::Scalar::from(u128)`；新增 `witness_u128_scalar_bytes` 仅用于对照 |
| 字节级单元测试（小整数） | ✅ | `curve::tests::embed_matches_witness_small_values` |
| 字节级单元测试（network A `weight.json` 178 项） | ✅ | `curve::tests::embed_matches_witness_network_a_weight_json` |
| 确定性 | ✅ | `curve::tests::embed_u128_is_deterministic` |

**结论：** 对当前 witness 权重集，承诺侧与 witness 侧 **32 字节标量完全一致**；L1 电路绑定仍待阶段 1。

**代码：** `src/cp-snark-full/src/curve.rs`

---

### 0.2 工程债（审查报告 P2a / P2b）

| 项 | 状态 | 说明 |
|----|------|------|
| `run_protocol.py` 将 `curveOrder` 误标为 $n_2$ | ✅ | 改为打印 `curveBaseField` → $n_2$、`curveOrder` → $q_2$ |
| `main.rs` setup 曲线字段打印 | ✅ | 同上语义 |
| `main.rs` prove 子命令变量名 | ✅ | `num_point_mults` / `num_point_adds`，与 `ClientChallenge::sample` 参数一致 |
| `gamma_add` / `gamma_mult` 死字段 | ✅ 已接入语义 | `layer_proof::common::challenge_for_stage`：卷积→`gamma`，池化→`gamma_add`，FC→`gamma_mult`（暂作 γ′） |
| `protocol.json` 诚实 coverage 标签 | ✅ | 新增 `proof_coverage`（默认 `ec_gadget_only`，旧 JSON 反序列化兼容） |

**代码：** `python/run_protocol.py`、`src/main.rs`、`src/challenge.rs`、`src/protocol.rs`

---

### 0.3 （可选）曲线嵌入回归测试

| 项 | 状态 |
|----|------|
| `curveBaseField == q_1` / `a_pd` 回归 | ⏸ 未做（路线图标注为建议项） |

---

## 多层计算量证明 — `src/cp-snark-full` 模块落点

> **说明：** 论文对卷积 / 平均池化 / FC 均有证明方法；激活由客户端执行，**无**服务器侧计算量证明。  
> 本次新增 **规范 + 标量 RLC 辅助 + 分阶段 Spec**；**完整 R1CS** 仍属路线图阶段 4.3（卷积 γ）、6.1（FC MAC）。

### 新增目录 `src/cp-snark-full/src/layer_proof/`

| 文件 | 论文对应 | 当前实现深度 |
|------|----------|----------------|
| `mod.rs` | §IV–V 总览、Remark | 导出 + 激活层**不实现**说明 |
| `common.rs` | γ / γ′ 分阶段、`ProofCoverage` | `challenge_for_stage`、`fold_rlc`（委托 `rlc.rs`） |
| `rlc.rs` | 式 (9)(10) 验证方 RLC | `conv_rlc_*`、`fc_rlc_*`、`mac_filter_window` |
| `verify.rs` | 式 (5)(7)(8) 逐格 + 式 (9)(10) 压缩 | `verify_conv_eq9_rlc`、`verify_pool_eq7_*`、`verify_fc_eq10_rlc` |
| `conv.rs` / `pool.rs` / `fc.rs` | 各层 Spec + gadget 调度 | `build_gadget_schedule`、`from_plaintext_conv` |
| `gadget.rs` | PtAdd / PtMul 槽位 | `LayerGadgetSchedule` |
| `stack.rs` | 线性层栈 | `ServerLinearProofStack::verify_all` |

**算法文档：** [`各层计算量证明算法-论文对齐.md`](./各层计算量证明算法-论文对齐.md)  
**代码包说明（含 verify vs prove、各层 I/O）：** [`src/cp-snark-full/src/layer_proof/README.md`](../src/cp-snark-full/src/layer_proof/README.md)  
**cp-snark-full 目标架构草案：** [`cp-snark-full-架构草案.md`](./cp-snark-full-架构草案.md)  
**M1 实现说明：** [`src/cp-snark-full/README.md`](../src/cp-snark-full/README.md)、[`model-trace-接口与卷积windows方案.md`](./model-trace-接口与卷积windows方案.md)

### M1 架构落地（2026-06-04）

| 模块 | 状态 |
|------|------|
| `commit/` | ✅ 重导出 `commitment` |
| `model/` | ✅ `ModelParams` + `model_export.json` |
| `trace/` | ✅ EC JSON + `build_linear_stack_optional` |
| `statement/` | ✅ `topology`、`check` 别名、`ProofCoverageV2` |
| `prove/` / `verify/` | ✅ `prover_pipeline` / `verifier_pipeline` |
| `protocol/` | ✅ `ProtocolArtifacts` v2 + 计时 |
| `circuit/mac_rlc` | ⛔ 停用桩（定稿；目标按层 π） |
| Python `convertFormatForRust_conv` | ✅ 导出 filter（windows 待补） |
| `model::ModelStore` + `model_store/` JSON | ✅ 接口 + 示例 `vpin-network-a` |
| [`task3-模型接入解析与存储方案.md`](./task3-模型接入解析与存储方案.md) | ✅ 设计参考（task3.txt） |

### 与现有子电路的关系

```text
[A] Server.py     →  rLCL/rLCR 式 (9)(10) 计算（权威）
layer_proof/*     →  标量预检（客户端仅 eq9/eq10，见设计定稿）
circuit_prove.rs  →  PtAdd/PtMult EC gadget（整批 [B]）
protocol.rs       →  编排 + proof_coverage
```

### 单元测试

| 测试 | 状态 |
|------|------|
| `layer_proof::conv::tests::conv_eq5_and_eq9_toy` | ✅ |
| `layer_proof::conv::tests::conv_plaintext_layout_matches_mac` | ✅ |
| `layer_proof::pool::tests::pool_eq7_toy` | ✅ |
| `layer_proof::fc::tests::fc_eq8_eq10_toy` | ✅ |
| `curve::*`（P0） | ✅ |

---

## 未完成（明确留到后续阶段）

| 路线图项 | 原因 |
|----------|------|
| 阶段 1.1 Pedersen 打开 / Spartan Comm | 未在本批次 |
| 阶段 1.2 L1：`witness_para_i = w_i` R1CS | 依赖 0.1 ✅ 后接电路增量 |
| 阶段 4.3 卷积式 (9) **R1CS** + transcript 跨进程 prove | 标量验证 ✅；电路未接 |
| 阶段 6.1 FC 式 (10) **R1CS** | 同上 |
| 从 Python `rust_files` JSON 填充各层 Spec | 需导出接口（见下方待确认） |
| 阶段 1 Pedersen / L1 | 用户要求**暂不**做 |

---

## 变更文件索引

| 路径 | 变更类型 |
|------|----------|
| `src/cp-snark-full/src/curve.rs` | P0 编码 API + 测试 |
| `src/cp-snark-full/src/layer_proof/*.rs` | 新建，分层证明 Spec |
| `src/cp-snark-full/src/lib.rs` | 注册 `layer_proof` |
| `src/cp-snark-full/src/challenge.rs` | γ 字段用途文档 |
| `src/cp-snark-full/src/protocol.rs` | `proof_coverage` |
| `src/cp-snark-full/src/main.rs` | CLI 命名与打印 |
| `src/cp-snark-full/python/run_protocol.py` | $n_2$ / $q_2$ 打印修正 |
| `docs/任务完成清单2026-6-4.md` | 本文件 |

---

## 修订记录

| 日期 | 内容 |
|------|------|
| 2026-06-04 | 完成阶段 0.1、0.2；新增 `layer_proof` 模块骨架；`cargo test` 4/4 通过 |
| 2026-06-04 | 各层论文算法标量验证 + gadget 调度 + 算法文档；`cargo test` 7/7 通过 |

---

## 待你确认（再接 R1CS / 填 witness 前）

1. **目标网络**：是否仅以 **network A** 层序/尺寸为唯一实例？（当前 API 为通用尺寸。）
2. **Witness 来源**：层 Spec 是否从 **E₂ 密文点 JSON**（`pointMult`/`pointAdd`）解码填充，还是继续用 **u128 标量槽** 对照？
3. **`proof_coverage` 升级时机**：是否在下一批把 `prover_run` 改为在 prove 前调用 `ServerLinearProofStack::verify_all`？
